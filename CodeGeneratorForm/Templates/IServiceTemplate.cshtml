@using  System
@using  System.Collections.Generic
@using  System.IO
@using  System.Linq
@using  SqlSugar
@using  CodeGeneratorForm
@{
    Table table =Model;
    string firstName=table.ClassName.First().ToString().ToLower() + table.ClassName.Substring(1);
    string privateFirstName = "_" + firstName;
}
namespace 命名空间;

public interface I@(table.ClassName)Service
{
    Task<List<Vo@(table.ClassName)>> FindList(Vo@(table.ClassName) vo, PageModel page = null);
    Task<long> Insert(Vo@(table.ClassName) vo);
    Task<List<long>> InsertBatch(List<Vo@(table.ClassName)> vos);
    Task<int> UpdateBatch(List<Vo@(table.ClassName)> vos);
    Task<int> Update(Vo@(table.ClassName) vo);
}